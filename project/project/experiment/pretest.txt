"What do you have to add to complete the definition of a main method?":::"___ static void main(String[] a){}":::["public"]
"What do you have to add to make this a definition and initialization of a variable?":::"___ a = 9;":::["char"::"short"::"long"::"byte"::"int"::"float"::"double"]
"Which operator do you have to use to print the result true?":::"int i = 1; int i=1; System.out.println(__i == 2);":::["++"]
"Which operator do you have to use to add 1 to a?":::"int a = 10; if(a%10 == 0 ___ a%5 == 0) {a = a+1;}":::["&&"]
"Which operator do you have to use print out true once?":::"___(1+1 == 2)System.out.println("true");":::["if"]
"What do you have to insert to reduce the variable i under 2?":::"int i=20;while(i >= 2)i=i<<2;":::["while"]
"Which keyword do you need to insert to make the code syntactically valid?":::"char a = 0;switch(a){default:;}":::["switch"]
"Define a array with 3 places.":::"int a[] = ___;":::["new int[3]"]
"Define the sum method that sums up all entries from an array.":::"public static int sum(int[] a) {int sum=0; for(int e: a)sum+=e; ___;}:::["return sum"]
"Define the class name, which stores a name as a string.":::"___ Name{String value = "";\npublic void setName(String a) {this.value = a;}\npublic String getName() {return this.value;}}":::["class"]